import React from 'react';
import { EffectWithoutArgs } from 'src/utils/components';

type PropsT = {};

export const SelectProjectRequestEffect: React.FC<PropsT> = (p: PropsT) => {
  return <EffectWithoutArgs f={() => {}} />;
};
